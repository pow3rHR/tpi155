function Persona(nombre, apellido) {
    this.nombre = nombre;
    this.apellido = apellido;
}

console.log(Persona);

/*const diego = new Persona("Diego", "Herrera");

console.log(diego.nombre);*/

// Los objetos constructor me proveen de una plantilla para crear muchos objetos del mismo tipo.
// También pueden tener comportamiento y valores compuestos en su estructura.